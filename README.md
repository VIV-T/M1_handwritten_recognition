# Image analysis

This repository contains the three project realized.

Notes : To build the python venv related to each project, execute the installation cmd : "pip install -r requirements.txt" in the target folder. 
